[package]
name = "house_or_hodl"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.7.1", features = ["fullstack"] }

[features]
default = ["web", "server"]
web = ["dioxus/web"]
server = ["dioxus/server"]
